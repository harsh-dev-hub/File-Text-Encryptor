# Encryptor – Python dependencies
# Install with: pip install -r requirements.txt

# GUI framework
PySide6

# Cryptography
cryptography

# Build tool (only needed for packaging, not at runtime)
pyinstaller
